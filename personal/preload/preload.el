;;; preload.el --- Emacs Preload: Preload configuration

;;; Commentary:

;;; Code:

;; Disable Flyspell (annoying)
(setq prelude-flyspell nil)

;; Disable guru-mode (annoying)
(setq prelude-guru nil)

;;; preload.el ends here
